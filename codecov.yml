coverage:
  status:
    project:
      default: false # disable the default project status
      primitives:
        flags:
          - primitives
        paths:
          - packages/reprimitivesact/
      forms:
        flags:
          - forms
        paths:
          - packages/forms/

flags:
  primitives:
    paths:
      - packages/primitives/
  forms:
    paths:
      - packages/forms/
